server:
  port: 8080
spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: false
netflix:
  appinfo:
    name: ${spring.application.name}
ribbon:
  eureka:
    enabled: false
