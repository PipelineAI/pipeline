# Lombok
org.projectlombok:lombok=1.16.+

# =============
#    Hystrix
# =============
HYSTRIX_VERSION=1.5.+

# Hystrix Dashboard
com.netflix.hystrix:hystrix-dashboard=$HYSTRIX_VERSION

# Hystrix Core
com.netflix.hystrix:hystrix-core=$HYSTRIX_VERSION

# Hystrix Metrics Stream
com.github.kennedyoliveira:hystrix-vertx-metrics-stream=$HYSTRIX_VERSION

# =============
#  END Hystrix
# =============

# Logging
SLF4J_VERSION=1.7.+
org.slf4j:slf4j-api=$SLF4J_VERSION

# Log Back
LOG_BACK_VERSION=1.+
ch.qos.logback:logback-core=$LOG_BACK_VERSION
ch.qos.logback:logback-classic=$LOG_BACK_VERSION

# Vert.x
VERTX_VERSION=3.+
io.vertx:vertx-web=$VERTX_VERSION

# Test
junit:junit=4.+
io.vertx:vertx-unit=$VERTX_VERSION
org.mockito:mockito-core=1.+
com.github.stefanbirkner:system-rules=1.+