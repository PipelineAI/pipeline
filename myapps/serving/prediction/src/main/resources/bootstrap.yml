server:
  port: 9040 
spring:
  application:
    name: prediction-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://master.pipeline.io:8761/eureka/
netflix:
  appinfo:
    name: ${spring.application.name}
  atlas:
    uri: http://master.pipeline.io:7101/api/v1/publish
