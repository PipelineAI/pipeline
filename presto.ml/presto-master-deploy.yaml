apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: presto-master
  labels:
    app: presto-master
spec:
  replicas: 1
  selector:
    matchLabels:
      app: presto-master
  template:
    metadata:
      labels:
        app: presto-master
    spec:
      containers:
      - name: presto-master
        image: docker.io/fluxcapacitor/presto-master-0.167:master
        resources:
          limits:
            cpu: 1000m
            memory: 10000Mi
          requests:
            cpu: 100m
            memory: 1000Mi
        imagePullPolicy: "Always"
        env:
        - name: GET_HOSTS_FROM
          value: env
        ports:
        - containerPort: 7060
        volumeMounts:
          - name: presto-catalog-volume
            mountPath: /root/presto-server-0.167/etc/catalog
      volumes:
        - name: presto-catalog-volume
          configMap:
            name: presto-catalog
