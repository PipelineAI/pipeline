/*
 * TriggerHttpForm Messages
 *
 * This contains all the text for the TriggerHttpForm component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  headerhttptrigger: {
    id: 'app.components.TriggerHttpForm.headerhttptrigger',
    defaultMessage: 'Http trigger',
  },
});
